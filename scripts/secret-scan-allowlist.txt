# Deliberate, reviewed example values that must remain in the repository.
# Format: <relative-path>:<pattern-name>  (see scan-secrets.py PATTERNS)
# Only add entries for non-secret example/default values, never for real credentials.
# backend/.env.example ships the documented local-development default database URL
# with the throwaway postgres/postgres credentials used by docker-compose.
backend/.env.example:database-url-with-password
# The bundle validator tests deliberately feed sensitive-shaped example values
# to prove the validator rejects them. The values are inert test fixtures.
backend/tests/unit/test_evidence_bundle_validator.py:private-key-block
backend/tests/unit/test_evidence_bundle_validator.py:openai-api-key
backend/tests/unit/test_evidence_bundle_validator.py:jwt-secret-value
backend/tests/unit/test_knowledge_release.py:openai-api-key
