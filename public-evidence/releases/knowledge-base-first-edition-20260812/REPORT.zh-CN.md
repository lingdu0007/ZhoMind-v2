# 知识库 First Edition 候选证据

## 当前候选

私有 authority 在六个主题下包含三十条经过人工 Knowledge Editorial Review 的 target entry。二十四条扩展 entry 已于 2026-08-12 获得人工批准，其 Claim-Evidence Link、snippet 与预先记录的 Gold Evidence 均已冻结。当前没有 target entry 发布；该候选不是 `First Edition`。

## 当前生产运行

当前 approved 资产完成了 30 条真实 Candidate Build、显式管理员发布、Knowledge Map 核对以及 120 个认证 production Compose/live-provider chat case。Gate 仅通过 5 个 case：direct、paraphrase 与 combined-condition 的完整 Gold Evidence top-three coverage 均为 0；30 个 Boundary Query 中 5 个正确拒答、16 个产生 unsupported answer、9 个因 provider unavailable 未完成。90 个 answerable case 中有 54 个 citation URL 可打开，但产品未暴露 provider-prompt snapshot identity，因此 exact snapshot coverage 为 0。provider 另有 32 个 rate limit 和 5 个 timeout。全部 30 条失败 entry 已通过管理员 `batch-delete` withdrawal seam 撤下，并验证活动文档与 Knowledge Map 均不再包含它们。

## 维护

记录的 maintenance run 实际打开 21 个唯一公开 source URL，没有产生 broken-link work；同时为 30 条 approved entry 创建人工 release comparison work，为季度抽样的 6 条失败 entry 创建 re-acceptance work。当前没有 draft、90-day stale 或 six-month Stable Principle work，自动发布数为 0。

## 限制

公开 bundle 只包含归一化计数、hash、revision 与限制。Private corpus prose、query、Gold Evidence、source snapshot、answer、excerpt、entry identity、user、host、credential 与完整 acceptance output 均被排除。

本次 run 验证了首个 `helpful` feedback 提交，但未证明四种 feedback label 的完整 live 契约；后续 runner 已收紧为必须验证四种 label、Knowledge Map 元数据和前三个 retrieval source 的 Gold Evidence coverage，需在下一次 production re-acceptance 中执行。
